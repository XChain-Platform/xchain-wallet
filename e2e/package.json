{
    "name": "@xchain-wallet/e2e",
    "version": "0.57.0",
    "private": true,
    "description": "Playwright end-to-end suite — drives the web SPA through the full Phase 1 flows. Pairs with the regtest stack for sign+broadcast specs once real xchain-sdk is bundled.",
    "type": "module",
    "scripts": {
        "test": "playwright test",
        "test:headed": "playwright test --headed",
        "install:browsers": "playwright install --with-deps chromium"
    },
    "devDependencies": {
        "@axe-core/playwright": "^4.10.0",
        "@playwright/test": "^1.48.0"
    }
}
