{
  "_comment": [
    "The dated release credentials, declared once so something can read them.",
    " §6 states these expiries in prose and called the first of them 'a",
    "renewal date rather than a fact to forget'. Nothing read it, which is §9's",
    "own defect one packaging over: a cadence that is prose is not a cadence.",
    "",
    "`expires` is the DECLARED date. `verifyFrom` names the artifact that",
    "actually carries it, and credential-expiry.mjs reads notAfter out of that",
    "artifact whenever it is reachable and FAILS on any drift from the",
    "declaration. Declaring without measuring is exactly how expected-artifacts",
    ".txt certified half-built releases ; a hand-maintained date file",
    "nothing checks would repeat it.",
    "",
    "Paths are `~`-relative on purpose: the release machine is where this",
    "material lives, and CI has none of it. There, the tool checks the declared",
    "dates and says in as many words that it could not measure them."
  ],
  "policy": {
    "renewalLeadDays": 60
  },
  "credentials": [
    {
      "id": "K3",
      "what": "Developer ID Application certificate: signs the direct-download macOS build",
      "expires": "2027-02-01T22:12:15Z",
      "verifyFrom": "~/.xchain-release/developer-id.cer.pem",
      "breaks": "every macOS direct-download release. Builds still sign with an expired leaf and Gatekeeper rejects them on every user's machine, which is a silent-at-build, loud-at-install failure",
      "note": "issued under Apple's G1 Developer ID CA, which itself expires at this exact second, so this date cannot be extended by a reissue under the same authority: the replacement comes from G2"
    },
    {
      "id": "K18",
      "what": "Apple Distribution certificate: signs the Mac App Store app",
      "expires": "2027-08-07T01:34:10Z",
      "verifyFrom": "~/.xchain-release/mas-distribution.cer",
      "breaks": "the Mac App Store lane (§13). A store build cannot be signed, so no submission is possible"
    },
    {
      "id": "K19",
      "what": "3rd Party Mac Developer Installer certificate: signs the store .pkg",
      "expires": "2027-08-07T01:34:11Z",
      "verifyFrom": "~/.xchain-release/mas-installer.cer",
      "breaks": "the Mac App Store lane at the package step specifically. K18 can sign the app perfectly and the .pkg still cannot be produced: they are two certificates and both are required"
    },
    {
      "id": "MAS-PROFILE",
      "what": "Mac App Store provisioning profile P9CWZT978L",
      "expires": "2027-08-07T01:34:10Z",
      "verifyFrom": "~/.xchain-release/xchain-wallet-desktop-mas.provisionprofile",
      "breaks": "the Mac App Store build. A profile names its certificates and dies with them, so a K18/K19 reissue requires recreating this too"
    },
    {
      "id": "MAS-DEV-PROFILE",
      "what": "Mac Developer provisioning profile PWABR9JC49, for the §13 hardware-signer proof",
      "expires": "2027-08-07T01:52:02Z",
      "verifyFrom": "~/.xchain-release/xchain-wallet-desktop-mas-dev.provisionprofile",
      "breaks": "the WebHID-under-sandbox proof (frontier row 13), which is the check that decides whether the store channel ships at all"
    }
  ]
}
