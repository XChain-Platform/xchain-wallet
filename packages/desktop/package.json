{
    "name": "@xchain-wallet/desktop",
    "version": "0.333.1",
    "license": "AGPL-3.0-or-later",
    "private": true,
    "description": "Electron desktop shell for XChain Wallet (Windows / macOS / Linux) - Phase 2 §40.12: main-process signing isolation (§9.3.2), OS keychain auto-unlock, WebHID hardware signer pairing, electron-builder packaging with Level-2 reproducible pre-signing artifacts (§51), URI scheme registration, and electron-updater wiring.",
    "main": "./main/index.js",
    "type": "module",
    "scripts": {
        "start": "pnpm run build:renderer && electron .",
        "build:renderer": "vite build",
        "build": "pnpm run build:renderer",
        "dist": "pnpm run build:renderer && electron-builder --config ./electron-builder.config.cjs",
        "dist:unpacked": "pnpm run build:renderer && electron-builder --config ./electron-builder.config.cjs --dir",
        "reproduce": "bash scripts/reproduce.sh"
    },
    "dependencies": {
        "@ledgerhq/hw-app-btc": "^10.21.0",
        "@ledgerhq/hw-transport-webhid": "^6.35.0",
        "@xchain-wallet/core": "workspace:*",
        "@xchain-wallet/extension": "workspace:*",
        "electron-updater": "^6.3.0",
        "lightweight-charts": "^4.2.0",
        "react": "^18.3.0",
        "react-dom": "^18.3.0"
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^4.3.0",
        "electron": "^41.3.0",
        "electron-builder": "^25.1.0",
        "vite": "^6.4.2"
    }
}
