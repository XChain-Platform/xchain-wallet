CAPACITOR_DEBUG = true

// The same tag-derived version numbers the Release config gets, so a debug
// build on a device is identifiable as the release it came from. Optional
// include: the file is generated by `pnpm --filter @xchain-wallet/mobile
// build` and deliberately never committed (rails §2).
#include? "App/Version.xcconfig"
